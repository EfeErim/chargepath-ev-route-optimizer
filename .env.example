# Optional self-hosted OSRM endpoint. The core and tests do not require network access.
# Public demo services must be selected explicitly and are never the default.
OSRM_BASE_URL=http://127.0.0.1:5000
